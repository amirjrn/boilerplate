name = "verify-otp"
main = "verify.js"  # Fixed: should point to verify.js
compatibility_date = "2025-12-02"

# For production: specify your domain and route pattern
# Uncomment and replace with your actual domain:
# routes = [
#   { pattern = "yourdomain.com/verify/*", zone_name = "yourdomain.com" }
# ]

# For development: enables *.workers.dev subdomain
workers_dev = true